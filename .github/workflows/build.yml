name: Build images

on:
  push:
    branches: [main]
    paths:
      - 'images/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

env:
  CI: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            platform: linux/amd64
            arch: amd64
          - runner: ubuntu-24.04-arm
            platform: linux/arm64
            arch: arm64
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push devshell (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: ./images/devshell
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/devshell:latest-${{ matrix.arch }}
            ghcr.io/${{ github.repository_owner }}/devshell:${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push postgres (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: ./images/postgres
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/postgres:17-${{ matrix.arch }}
            ghcr.io/${{ github.repository_owner }}/postgres:17-${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push webtest (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: ./images/webtest
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/webtest:latest-${{ matrix.arch }}
            ghcr.io/${{ github.repository_owner }}/webtest:${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push ollama (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: ./images/ollama
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ollama:latest-${{ matrix.arch }}
            ghcr.io/${{ github.repository_owner }}/ollama:${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push mineru (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: ./images/mineru
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mineru:latest-${{ matrix.arch }}
            ghcr.io/${{ github.repository_owner }}/mineru:${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  merge-manifests:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Create and push multi-arch manifests
        run: |
          SHA="${{ github.sha }}"
          SHA_SHORT="${SHA:0:7}"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/devshell:latest" \
            "ghcr.io/${{ github.repository_owner }}/devshell:latest-amd64" \
            "ghcr.io/${{ github.repository_owner }}/devshell:latest-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/devshell:${SHA_SHORT}" \
            "ghcr.io/${{ github.repository_owner }}/devshell:${SHA}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/devshell:${SHA}-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/postgres:17" \
            "ghcr.io/${{ github.repository_owner }}/postgres:17-amd64" \
            "ghcr.io/${{ github.repository_owner }}/postgres:17-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/postgres:17-${SHA_SHORT}" \
            "ghcr.io/${{ github.repository_owner }}/postgres:17-${SHA}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/postgres:17-${SHA}-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/webtest:latest" \
            "ghcr.io/${{ github.repository_owner }}/webtest:latest-amd64" \
            "ghcr.io/${{ github.repository_owner }}/webtest:latest-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/webtest:${SHA_SHORT}" \
            "ghcr.io/${{ github.repository_owner }}/webtest:${SHA}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/webtest:${SHA}-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/ollama:latest" \
            "ghcr.io/${{ github.repository_owner }}/ollama:latest-amd64" \
            "ghcr.io/${{ github.repository_owner }}/ollama:latest-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/ollama:${SHA_SHORT}" \
            "ghcr.io/${{ github.repository_owner }}/ollama:${SHA}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/ollama:${SHA}-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/mineru:latest" \
            "ghcr.io/${{ github.repository_owner }}/mineru:latest-amd64" \
            "ghcr.io/${{ github.repository_owner }}/mineru:latest-arm64"
          
          docker buildx imagetools create -t \
            "ghcr.io/${{ github.repository_owner }}/mineru:${SHA_SHORT}" \
            "ghcr.io/${{ github.repository_owner }}/mineru:${SHA}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/mineru:${SHA}-arm64"
